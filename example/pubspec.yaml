name: app_health_sdk_example
description: Demonstrates how to use the app_health_sdk plugin.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.10.1
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  app_health_sdk:
    path: ../

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
